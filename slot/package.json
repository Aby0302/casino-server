{
  "name": "casino-server",
  "version": "1.0.0",
  "description": "Casino slot server with cloud streaming and admin panel",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "docker:build": "docker compose build",
    "docker:up": "docker compose up -d"
  },
  "dependencies": {
    "playwright": "^1.61.1",
    "ws": "^8.21.1"
  }
}
